name: Generate Music Wave

on:
  schedule:
    - cron: '0 0 * * *' # Chạy mỗi ngày một lần để làm mới style (ngoài ra nó sẽ chạy khi push)
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
          
      - name: Generate SVG
        run: python generate_wave.py
        
      - name: Commit and Push
        run: |
          git config --global user.name "locfaker"
          git config --global user.email "locv2659@gmail.com"
          git add assets/music-wave.svg
          git diff --quiet && git diff --staged --quiet || (git commit -m "🎵 Update animated music wave visualizer" && git push origin main)
